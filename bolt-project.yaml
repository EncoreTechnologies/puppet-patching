# Usage:
#  bolt puppetfile install
#  bolt plan run patching::available_updates
name: patching

# paths are relative to this bolt.yaml file
modulepath: "./modules:./site-modules"
inventoryfile: "./bolt/inventory.yaml"
log:
  './bolt.log':
    level: debug
    append: true
  console:
    level: notice
